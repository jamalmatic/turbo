import LoginFormCore from "./LoginFormCore";
import LoginFormLayout from "./LoginFormLayout";

const LoginForm = () => {
    return (
        <LoginFormLayout>
            <LoginFormCore/>
        </LoginFormLayout>
    );
}

export default LoginForm;